#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

int pop_listint(listint_t **head)
{
	listint_t *tmp;

	tmp = *head;
	for (tmp = tmp->next; head != NULL; tmp = tmp->next)
	{
		free(tmp);
	}
	tmp = *head;
	printf("%d", tmp->n);
	return (tmp->n);
}
