#include <stdlib.h>
#include <stdio.h>
#include "calc.h"


int (*get_op_func(char *s))(int a, int b)
{
	op_t ops[]={{"+", op_add},{"-", op_sub},{"*", op_mul},{"/", op_div},{"%", op_mod},{NULL, NULL}};
	//int i;

	printf("%s  %s\n", ops->op, s);

/**	for (i = 1; i < 5; i+= 2)
	{
		if (ops.op[1] == s)
		{
			printf("OK\n");
			return (1);
		}
	}
	printf("Error\n");**/
	return (0);
}
